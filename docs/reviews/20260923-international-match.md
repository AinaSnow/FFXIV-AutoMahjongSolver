# 国际服 2026-09-23 实机检查

本报告为证据审查，不宣称已修复或已验证网络协议。原始日志保持原样；本文不包含玩家名单、网络载荷或内存内容。

## 样本

- 客户端：2026.09.15.0000.0000，插件构建 ID：cf138408-32ec-4d8f-84d9-37cbd2f9baf6。
- 北京时间：16:23:18 入桌，16:39:50 离桌。
- 原始录包：debug-packets/capture-20260923-082318-360-3b25e00620cd40fc8611d22ef8be541c.ndjson。
- UI 归档：match-archives/match-20260923-083950Z；辅助记录：inputs/inputs-20260923.ndjson、findings/findings-20260923.ndjson 与对应 memdumps。
- 用户明确说明最后一手手动点击。UI 最终分数为 [48000, 5400, 27000, 19600]，相对自身第 1 名；不能把这一场作为全自动成功或策略胜率改善证据。

## 1. 自动录包没有截获数据（阻断协议验证）

641 字节文件只有 capture-start / capture-end，packets=0、dropped=0、rejected=0。首尾时间覆盖全场，却没有回调数据或读取拒绝。因此不是载荷解析失败或队列溢出。当前函数指针 Hook 没有在这场产生接收观察；可能存在绕过被替换虚表槽位的调用路径，具体原因仍需验证，不能仅凭这一文件断言调用实现。

尾部 stream_complete=true 只描述队列排空，却容易使空采集看似正常。现有审计脚本会正确报告 no_packets。需要恢复能够覆盖实际调用路径的接收入口，并在入桌后短时间无任何回调时明确报警；零包采集不能显示为健康完成。不要启用旧 opcode 模板。

## 2. 最后一次自动自摸未获得可靠执行确认（阻断全自动验收）

| 北京时间 | 观察 |
|---|---|
| 16:39:14.938 | 第一次自动 Tsumo，option=0，state=6，派发返回 Ok |
| 16:39:19.954 | 等待 5015 ms，仍是 state=6，dispatch_outcome.commit=false |
| 16:39:20.577 | 第二次自动 Tsumo，仍返回 Ok |
| 16:39:22.358 | 输入回调 [11,0]，pre/post 内存快照显示 state=6 → 19 |
| 16:39:22.374 | 状态变化被当前观察器记录为第二次自动派发 commit=true |
| 16:39:32.969 | 输入回调 [17]，state=32 → 29 |
| 16:39:36.892 | 自动下一局回调 [14] |
| 16:39:50.662 | 离桌回调 [-2] |

第一、二次自动 Tsumo 时间点之间没有对应的 FireCallback 输入记录。结合用户手动点击说明，16:39:22 的转换与人工接管一致，不能归功于第二次自动派发。该局输入 Hook 实际记录了 214 次回调，不能用此前初始化报错直接解释本次自摸失败。

代码对 state=6 的动作可能走 SelectItem 路径；这条路径的无效派发是重点排查对象。当前 findings 的 path 始终取 LastDiscardPath，所以自摸记录显示旧的 opcode-15+7 舍牌路径，不能据此认定自摸实际走了该分支。需要记录真实动作路径，并区分派发返回、状态变更及用户接管；不能盲目把捕获到的内部回调等同于所有 UI 路径的点击替代。

## 3. 摘要和手局结算存在记录错误（阻断胜率统计）

- summary 写入 8 个手局文件，但第八份只有空手的 state=35 / 27 及结算分数，没有新的可打手牌。结束时的牌山重置触发了虚假新局。
- 第一份 hand-start 的分数为全零，随后正常起始分数为四家各 25000。下一份 hand-end 因此生成四家各 +25000 的不合法结算。
- 新局边界拿到的是尚未更新的旧分数，后续 hand-end 因而存在一局延迟；最后 +13000 的自摸结算没有形成正确 hand-end。
- failed_actions=0 仅来自 action.result，没有纳入已出现的 commit=false。113 条 action 都返回 Ok，不代表 113 次被游戏执行成功；findings 有 110 条 outcome，其中 1 条明确未提交，其余状态转换亦不能证明因果来源。
- 全场记录的动作 hand_id 都为 1；UI 模式下手局标识还没有随实际手局推进。需要与回放/取消边界一起验证。
- UI 记录只有河牌数量，河牌种类/立直等公开信息未被完整观测；Mortal 本场未运行。零超时并不代表 Mortal 性能已验证。

需要在真实配牌后确认新局，等待可靠结算分数，不把离桌重置当新局；统计应明确区分派发尝试、超时、状态变化与手动接管。不能从这份摘要推算和牌率、放铳率或自动完成率。

## 可确认的数据与后续验证

UI 末尾分数相对自身为 48000、第 1 名。共记录 300 次决策（含重复局面发布），摘要平均决策耗时约 5.40 ms、P95 约 14.98 ms。零条 policy-error，日志摘要未报告写入失败。这些仅是本场本地观察，不是对照评测。

先修并验证接收覆盖/空录报警、自摸派发与执行归因、手局边界/结算记录。下一次先短时入桌检查 Saved 是否增长，避免再用完整一场发现空录。仍不启用当前版本网络推理，也不宣称提高胜率。
