# 2026-09-24 训练素材首场实测

19:50–20:05（UTC+8）的 `match-20260924-115021Z`，4 小局，最终 26,500 分、第 2 名。比赛身份 `b1ecb4582007943e9b2895b8f6ba957f41d41611b23dd15d72d9194c34834255`。

## 已确认

- 长期归档成功，逐文件 SHA-256 校验通过，策略权重快照与哈希一致。
- 549 条麻将网络事件、676 条发送 MJAI、64 次模型响应、62 次 Mortal 决策。决策来源另外有 35 次本地接管及 6 次终局确认。
- Mortal 的 62 条决策均记录模型身份。权重 SHA-256 为 `0a88ddad649804d085491b5397d895f596b0e55f30632c549ea145bb44786563`，实际加载 95,392,490 字节，架构版本 4。
- 110 次操作均有结果记录：107 次记录到回调及状态变化、2 次被后续操作替代、1 次结果等待超时。不能把这些结果等同于服务端逐条确认。
- 有 3 次 Mortal 决策超时。第三小局在配牌处因“不能通过分数守恒确认供托为零”隔离并使用本地策略，第四小局恢复。未修改有限试用的协议范围。

## 发现与修复

1. 录制在 20:05:26 停止，20:05:29 再次开启，退桌时只归档了最后 12 包的尾段。主段的 940 包原始文件仍在，无丢弃或拒绝。现在按一桌收集全部录制段并逐段封口，记录中断与分段标记。
2. `FireCallback` 可同步更新聚合局面。原动作日志读取操作后的 `Latest.Revision`，常比推荐的版本多 1，只有 20/110 次动作能严格关联推荐。现在调度回调在操作前冻结日志上下文，动作使用操作前手局和局面编号。日志 schema 升为 8；旧记录保留原貌并标记未知，不根据相邻编号猜配。
3. 导出原先只有已知初始庄家时才算名次，即使分数没有并列。这场因此得到空名次。现与核心名次算法一致：只有自身分数并列时才要求初始座位顺序。本场导出名次已为 2。
4. 文件正常封口不证明包含主要对局数据。导出新增网络事件覆盖检查；这场原有尾段副本明确标记 `raw_archive_packets_missing`。

## 历史素材保全

主段按“100 ns UTC 时间、opcode、完整 payload”核对，覆盖归档全部 549 条麻将事件。主段 SHA-256 为 `597891da19374d4bd04d4c5e4314192ae6cbb7e004af8d0def9468e9c856cb27`。

补充副本保存在插件配置目录 `training-data/supplements/match-b1ecb4582007943e9b2895b8f6ba957f41d41611b23dd15d72d9194c34834255/`。`supplement.json` 记录父归档 manifest 哈希、匹配依据及补充文件哈希。原封存目录、比赛身份和分组均未改写。当前导出器不自动合并该补充目录，仍如实报告原副本缺陷；后续重建训练数据时可以显式使用补充文件。

本场散列分组为 acceptance。本轮只诊断采集和数据管道，没有用比赛结果调模型或策略权重。数据仍标记不能直接训练。

## 验证

- 489 项插件测试、28 项 Python 测试通过。新增抓包关闭再开启、多段归档、操作回调立即变更局面、缺少主段但尾段正常封口、无同分名次及同分未知座位用例。
- 真实导出 `artifacts/training-export-20260924-live-check/`：10 场，970 条动作，其中本场 110 条。train 92、development 57、acceptance 110、unassigned 711；可直接训练数量仍为 0。
- 保留旧样本的精确关联限制；修复影响之后的日志。schema 8 的修复尚未经过新实机对局验证。
