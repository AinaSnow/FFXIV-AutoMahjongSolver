# 国际服 2026-09-24 实机检查

本次仅审查本地采集证据；不改写原始文件，不标记协议已验证。

## 样本

- 北京时间：2026-09-24 01:33:03–01:49:05。
- 客户端：2026.09.15.0000.0000 / Emj。
- 插件构建 ID：d8bce8cc-b73c-434d-ba92-6e84f17799a6。
- 录包：capture-20260923-173303-177-85accd84f7c045e995dc70545be5a9ff.ndjson（712 字节）。
- 归档：match-20260923-174905Z；对应 inputs/findings 使用 UTC 日期 20260923。
- 原始录包 SHA-256：6b2371bf523eeb038d643353c763dda16082d35d129ff0f863a7ba42fdea5ee1。

## 录包：入口已收到回调，包头读取仍阻断

文件声明 hook_mode=function-entry，尾部 packets=0、dropped=0、rejected=1262、last_rejection=invalid-segment-header。与上一场 rejected=0 不同，接收回调已触发，但没有一条包通过读取校验。no_packets=true、stream_complete=false 正确反映失败，没有再把空录标为成功。

审计工具报告 no_packets、transport_read_rejected、incomplete_capture。无有效载荷，不能分析麻将 opcode、校准字段或启用 Mortal。

当前 TryCopy 从 IPC 指针前 16 字节读取 32 字节头，并合并报告读取失败、长度、段类型、目标 ID、IPC 标记不符。现有文件不包含各字段的失败值，不能确定是不可读内存还是哪项布局假设不成立。Dalamud 本地 NetworkMonitorWidget 在同一入口只读取 packet+2 的 opcode，不证明前置段头或长度可用。下一步应拆分失败原因并增加有限的本地头部诊断，核实此入口是否保留长度信息；若没有，应使用携带已知缓冲区长度的上游接收边界。不能取消长度校验或猜固定载荷长度。无需再靠完整对局复现零包。

## 自动操作：自摸路由已获得本场实机证据

- 01:38:59.311，自动输入 [11,0]；01:38:59.315，自摸状态由 6 变 19，耗时约 3 ms。
- 01:39:00.874，自摸二次确认再次记录自动 [11,0]，该动作最终记录 state-changed。
- 01:41:44.660，自动荣和 [11,0]；约 8 ms 后状态由 15 变 19。01:41:46.230 自动二次确认，01:41:48.838 状态变 32。
- 89 条动作均有 outcome：87 条 state-changed、2 条 superseded，未记录 timeout 或缺失 outcome。state-changed 是同步自动回调和 UI 变化的关联，不等于网络确认。
- 两条 superseded 均为立直按钮（action_id 25、44），分别在约 3.21 s、2.81 s 后由 riichi-tsumogiri 舍牌替代；随后舍牌状态转 30。此状态表示独立按钮确认被后续步骤覆盖，不能直接判定立直失败，也不能证明服务端接受立直。后续可改为分阶段追踪立直流程。
- 输入源共 147 条 automation、27 条 external-or-game；后者包含游戏或其他来源，不能一概称为用户手动操作。

## 结算与本地统计

5 份手局文件均各有一次起手、一次结算；动作 hand_id 依次为 1–5，没有虚假第六局或全零起始分。自身每局分差为 [-2000,2700,5200,0,0]，最终分数 [30900,10500,31400,27200]，自身第 2 名、净增 5900 分。结算类型仍为 UI 分差推断（result_inferred=true）。

245 次决策，243 次 stable、2 次 terminal-guard；平均 4.23 ms、P95 15.61 ms；无 policy-error、归档未报告 IO 失败。Mortal 没有处理包或运行推理；本场名次不能作为策略提升的对照证据。

本次未修改运行代码，因此不重复运行测试。录包审计、逐手结算、动作及输入时间线已交叉检查。

## 02:20 短时诊断：前置段头假设不成立

用户热重载后采集了 02:20:03–02:20:18 的短样本，文件 capture-20260923-182003-227-14eaf0b5435849189d234cce5e83fb15.ndjson，2360 字节，schema 2，构建 ID 0d58fdab-6114-4434-aa8b-c80737470281。SHA-256：92b451b7df8932c107d27b7120784c7ce0ba9780f12c6706a682b0fdafe458ad。

记录为 1 个开头、3 个诊断、1 个结尾，无有效网络包。16 次拒绝包含 1 个 pre-roll-incomplete 标记和 15 次 invalid-segment-length；3 个诊断包含该预缓冲标记及 2 个候选头样本。diagnostic_dropped=0、diagnostic_unsampled=13，计数一致，正常以 disabled 封口。审计未报告格式或计数问题，但仍报告 no_packets、transport_read_rejected、incomplete_capture。

两份候选头均显示：IPC 指针本身起始标记为 0x14，+2 位置的候选 opcode 分别为 0x03D9、0x0242；这符合本地 Dalamud NetworkMonitor 读取 opcode 的位置。原先假定的 IPC-16 段长却为 0，同时段类型及目标 ID 校验不符。因此当前样本不具有实现所假定的“16 字节网络段头紧邻 IPC 头”布局。前置字节可能是对象或缓冲区管理信息，其具体类型尚未确认，不将其解释为其他有效字段，也不推断这些 opcode 的消息名称。

这是当前采集实现对入口布局的错误假设，不是用户没有正确开始抓包，也没有证据表明继续打一整场能解决。不能把 length=0 改为固定长度、只保留 IPC 标记校验或猜测另一个偏移后复制载荷。两份诊断中没有可靠载荷长度，尚不能直接构造完整网络包。

后续方向是从具有真实帧/段长度的上游边界取得长度，再与解码后的 IPC 对应，或接入 Deucalion 输出。其 [recv 实现](https://github.com/ff14wed/deucalion/blob/main/deucalion/src/hook/recv.rs) 在上游解压后处理帧；[packet 实现](https://github.com/ff14wed/deucalion/blob/main/deucalion/src/hook/packet.rs) 从帧解析长度，并在需要时保留长度用于后续解码重建。这支持更换采集边界的方向，不等于已经验证该版本 Deucalion 与本机客户端兼容。

本次仅审计、核对本地接收函数签名及上游源码，并记录结论；没有改动或部署运行代码。原始文件不修改，无需继续采集同类样本。

## 02:43 Deucalion 首次有效采集与部分协议对照

样本 capture-20260923-184352-728-c8049526a6a44303ac64701c22a1f28a.ndjson，38187 字节，SHA-256：f2489dbfa351bc59976cca953f9a4ea68065ce53e088e178f395fe165947b863；客户端 2026.09.15.0000.0000 / Emj，构建 ID 6da14585-0f51-4cb4-abe4-56c6339f6110。接收窗口北京时间 02:43:52.445–02:44:33.051，02:44:33 关闭开关正常封口。

71 个有效包、9 种 opcode，dropped/rejected/malformed 均为 0；schema 3 审计 blockers 为空。Deucalion 1.5.0 的本机日志显示成功初始化、订阅 MahjongSolver、仅筛选 Zone receive，并在关闭录包后退出，没有 WARN/ERROR。这是新捕获后端的实际成功证据；stream_complete 不等于完整对局或协议验收。

| 候选用途 | 当前 opcode | 载荷长度 | 样本数 | 已核对范围 |
|---|---|---:|---:|---|
| 对局开始（636） | 0x023F | 48 | 1 | 顺序位于配牌前；四家起始 25000，与 UI 相符；modeRaw=3 的比赛长度含义尚未确认 |
| 配牌（637） | 0x0133 | 104 | 1 | 自身索引 0，起手 13 张及分数与 UI 一致；只覆盖本场第一局 |
| 摸牌/鸣牌通道（638） | 0x02BB | 24 | 18 | 本段全部 action=0x100 摸牌，自身 5 次已对照；实际鸣牌分支尚未采到 |
| 弃牌（641） | 0x0214 | 32 | 17 | 自身 5 次弃牌均与动作及 UI 变更对应；包含 0x110/0x112，未包含立直 0x111 |

初始手牌、5 次自身摸牌、5 次自身弃牌，共 11 个阶段均与 1 秒内 UI 手牌（包括红牌标记）匹配，延迟约 27–338 ms。本段所有自身红牌标记为 false，不能据此验证红五编码。18 次摸牌与 17 次弃牌的顺序可解出 37 个 MJAI 事件；对手摸牌继续输出未知牌，不使用其他包推断对手暗手。

候选表保存为 data/protocols/20260915-international-candidate.json，verified=false。37 条选择后的麻将载荷和 11 份 UI 期望保存为纯逻辑回归样本；排除玩家实体 ID、IPC 头、名单及全部未归类载荷。124 项 Plugin.Game 测试通过，包括真实短片段回放。此回放未合成局末或对局结束事件，没有启用运行中的协议。

仍缺局末结果消息、跨局变化、其他自身座位、实际鸣牌、立直、红五、杠及追加宝牌、本场供托等证据。尤其目前 C# 解码器仍对本场供托填旧的 0，且庄家/局号语义需跨局验证，不能仅替换这四个 opcode 就宣称 Mortal 已恢复。

下一次使用现有运行版本，在匹配前开启录包，保持到完整对局结束并离桌；不必热重载。正常游戏即可，不强行制造鸣牌或杠。该样本已足以结束传输入口排查，后续采集用于补全麻将语义。


## 02:50–03:09 中途恢复采集：四局连续回放

样本 capture-20260923-185058-311-6b1a093ec0dd45bf948afbf1f3412fef.ndjson，688869 字节，SHA-256 d933f96c46d11f3f536070cd990ee92cb04f7a41f7c14f629d24e6b1d06b26eb。实际接收 02:50:59.329–03:09:49.880，离桌正常封口，1435 个包、26 种 opcode，dropped/rejected/malformed 均为 0。客户端仍为 2026.09.15.0000.0000，本段 UI 变体是 EmjL；早先短片段是 Emj，不能省略变体匹配。增加独立 EmjL 候选表，两张表均保持 verified=false。

中途开始的第 3 局缺少起手与此前历史，不尝试拼接失落时段。第 4–7 局各有配牌与结算：手局索引 [2,2,2,3]，自身当前风位索引 [2,2,2,1]。对齐后的起始分、13 张手牌及首张宝牌显示均相符；UI 配牌较网络晚约 1.35–1.52 秒，原来短片段使用的 1 秒界限不适合此样本。

| 新证据 | 数量/取值 | 结论与限制 |
|---|---|---|
| 0x016D / 504 字节 | 4 次 | 局末结果 B 候选（640），本段对应荣和；仅解码 end_kyoku，不宣称和牌字段全确认 |
| 0x0371 / 256 字节 | 1 次 | 局末结果 A 候选（639），本段对应自摸；同样只用于局末边界 |
| 配牌偏移 12 的 int32 | 0、1、2、0 | 与同庄连庄及二本场 5200+600 的结算吻合，修正 Honba 固定为 0；UI 的 seat_info_known=false，不能拿 UI 默认 0 当独立计数证据 |
| 红五筒编码 | 53（13×4+1） | 03:00:37.833 摸入、03:00:38.951 打出，UI 红牌布尔值前后对照确认；修正原先解成普通五筒的问题 |
| 吃/碰 | 8 / 10 次 | 均为对手；逐次可与前一弃牌匹配；0xA10 为鸣牌后弃牌 |
| 立直 0x111 | 3 次（完整四局内 2 次） | 自身一次与自动立直及舍牌对应；供托扣除时序仍缺独立 UI 证据 |
| 杠相关 | 0x400 两次、0x130 一次、0x200 三次、0x212 三次 | 时间顺序支持明杠/暗杠/补牌/补牌后弃牌候选；没有自身杠或加杠，本次不启用这些布局 |

559 条麻将载荷保存为 tests/Mahjong.Plugin.Game.Tests/RegressionFixtures/20260924-later-hands.json，不保存玩家名称、实体 ID、IPC 头或名单。前 72 条是无开局的片段，测试要求不输出伪造局面；后四局的初始手牌、57 次自身摸牌、57 次自身弃牌共 118 个检查点全部与 UI 匹配，4 次结果分数与 UI 结算匹配。测试显式保留并拒绝 9 条尚未支持的杠相关事件，不把自身手牌一致率称为完整公开局面回放通过。不能用这个测试宣称牌山、宝牌、对手副露已完整。

解码器原本静默跳过不认识的摸牌/鸣牌，并把任何弃牌动作当作普通手切。这会让 Mortal 继续使用缺失的历史。现改为明确抛出数据错误，Mortal 隔离当前局，公开局面标记不完整；下次真实配牌才恢复。此行为不等于杠支持已经完成。

整场 UI 归档为 match-20260923-190949Z，共 7 局，最终 [10000,15200,32100,42700]，自身第 4 名。125 条动作都有结果：118 条 state-changed、6 条 superseded、1 条 unconfirmed-state-change，无 timeout；未确认项是 02:45:41 的立直后舍牌，位于两段录包之间，无法从本次网络记录判定原因。321 次决策（320 stable、1 terminal-guard），均值 4.18 ms、P95 12.32 ms。Mortal 未运行，本场不是策略改进的对照实验。

验证：完整解决方案 881 项 .NET 测试通过（Plugin.Game 134 项、Dalamud 395 项）。未启用实时协议。仍需落实杠补牌与新增宝牌、自身杠回执/加杠、非零供托、红五万物理编码、场风变化和比赛长度；重复采集相同普通摸切不能替代这些验证。

另发现 UI 宝牌显示与网络指示牌相差一位。[官方说明](https://na.finalfantasyxiv.com/lodestone/playguide/contentsguide/goldsaucer/doman-mahjong/play_mahjong/)允许直接显示宝牌或显示传统指示牌。现有 UI 读取没有区分，必须核对显示模式再统一转换，不能把两者直接混用。已检查三次杠附近的本地快照，当前单字段 UI 读取没有反映新增宝牌；邻近内存与已采集 agent/Atk 数组中也未找到可交叉确认的连续宝牌序列，不能靠猜偏移启用。
